import React from 'react'import {useEffect, useState} from 'react'export const Clock: React.FC = () => {   const num2DigitsString = (num: number) => num < 10 ? '0' + num : num   const [date, setDate] = useState(new Date())   const hours = num2DigitsString(date.getHours())   const minutes = num2DigitsString(date.getMinutes())   const seconds = num2DigitsString(date.getSeconds())   useEffect(() => {      setInterval(() => {         setDate(new Date())      }, 1000)   }, [])   return (      <div className="App">         <h1>{`${hours}:${minutes}:${seconds}`}</h1>      </div>   )}